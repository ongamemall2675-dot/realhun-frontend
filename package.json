{
    "name": "realhun-frontend",
    "version": "1.2.0",
    "description": "혜화72 스마트 오피스 Frontend",
    "scripts": {
        "build": "rm -rf dist && mkdir -p dist && cp -r index.html apps assets admin realestate _routes.json dist/ && echo 'Build completed!'"
    }
}
